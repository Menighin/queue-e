extends layout

block content
  h1= title
  p Welcome to #{title}. It works
  div#test

  
  script.
    let socket = io('/queue-progress');
    socket.on('update', (data) => {
        document.getElementById('test').innerHTML += data + '<br>';
    });
    
